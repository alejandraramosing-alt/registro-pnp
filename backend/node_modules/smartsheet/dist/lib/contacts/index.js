"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createContacts = createContacts;
function createContacts(options) {
    const requestor = options.requestor;
    const baseUrl = options.apiUrls.contacts;
    const optionsToSend = Object.assign({ url: baseUrl }, options.clientOptions);
    const getContact = (options, callback) => {
        const urlWithSlash = baseUrl + '/';
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), { url: urlWithSlash }), options), callback);
    };
    const listContacts = (options, callback) => requestor.get(Object.assign(Object.assign({}, optionsToSend), options), callback);
    return {
        getContact,
        listContacts,
    };
}
