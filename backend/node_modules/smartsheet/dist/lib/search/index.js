"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSearch = void 0;
const apis_1 = require("../utils/apis");
const createSearch = (options) => {
    const requestor = options.requestor;
    const optionsToSend = Object.assign({ url: options.apiUrls.search, urls: options.apiUrls }, options.clientOptions);
    const searchAll = (getOptions, callback) => {
        const options = Object.assign(Object.assign({}, optionsToSend), getOptions);
        return requestor.get(options, callback);
    };
    const searchSheet = (getOptions, callback) => {
        const options = Object.assign(Object.assign({}, optionsToSend), getOptions);
        options.url = apis_1.apiUrls.search + '/sheets/' + getOptions.sheetId;
        return requestor.get(options, callback);
    };
    return {
        searchAll,
        searchSheet,
    };
};
exports.createSearch = createSearch;
