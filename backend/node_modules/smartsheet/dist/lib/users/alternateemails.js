"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.create = create;
function create(options) {
    const requestor = options.requestor;
    const optionsToSend = Object.assign({}, options.clientOptions);
    const addAlternateEmail = (postOptions, callback) => {
        const urlOptions = { url: buildUrl(postOptions.userId) };
        return requestor.post(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), postOptions), callback);
    };
    const getAlternateEmail = (getOptions, callback) => {
        const urlOptions = { url: buildUrl(getOptions.userId, getOptions.alternateEmailId) };
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), getOptions), callback);
    };
    const listAlternateEmails = (getOptions, callback) => {
        const urlOptions = { url: buildUrl(getOptions.userId) };
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), getOptions), callback);
    };
    const makeAlternateEmailPrimary = (postOptions, callback) => {
        const urlOptions = {
            url: buildUrl(postOptions.userId, postOptions.alternateEmailId) + '/makeprimary',
        };
        return requestor.post(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), postOptions), callback);
    };
    const deleteAlternateEmail = (deleteOptions, callback) => {
        const urlOptions = { url: buildUrl(deleteOptions.userId, deleteOptions.alternateEmailId) };
        return requestor.delete(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), deleteOptions), callback);
    };
    const buildUrl = (userId, alternateEmailId) => {
        return (options.apiUrls.users +
            '/' +
            userId +
            '/alternateemails' +
            (alternateEmailId !== undefined ? '/' + alternateEmailId : ''));
    };
    return {
        addAlternateEmail,
        getAlternateEmail,
        listAlternateEmails,
        makeAlternateEmailPrimary,
        deleteAlternateEmail,
    };
}
