"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.create = create;
const underscore_1 = __importDefault(require("underscore"));
function create(options) {
    const requestor = options.requestor;
    const optionsToSend = {
        url: options.apiUrls.templates,
    };
    underscore_1.default.extend(optionsToSend, options.clientOptions);
    const listUserCreatedTemplates = (getOptions, callback) => requestor.get(underscore_1.default.extend({}, optionsToSend, getOptions), callback);
    const listPublicTemplates = (getOptions, callback) => {
        const urlOptions = { url: options.apiUrls.templatesPublic };
        return requestor.get(underscore_1.default.extend({}, optionsToSend, urlOptions, getOptions), callback);
    };
    return {
        listUserCreatedTemplates: listUserCreatedTemplates,
        listPublicTemplates: listPublicTemplates,
    };
}
