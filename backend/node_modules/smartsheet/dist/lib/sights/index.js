"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSights = void 0;
exports.create = create;
const share_1 = __importDefault(require("../share/share"));
function create(options) {
    const requestor = options.requestor;
    const optionsToSend = Object.assign({}, options.clientOptions);
    // Legacy shares module (deprecated)
    const shares = (0, share_1.default)(options.apiUrls.sights);
    const getSight = (getOptions, callback) => {
        const urlOptions = { url: buildUrl(getOptions.sightId) };
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), getOptions), callback);
    };
    const listSights = (getOptions, callback) => {
        const urlOptions = { url: buildUrl() };
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), getOptions), callback);
    };
    const deleteSight = (deleteOptions, callback) => {
        const urlOptions = { url: buildUrl(deleteOptions.sightId) };
        return requestor.delete(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), deleteOptions), callback);
    };
    const updateSight = (putOptions, callback) => {
        const urlOptions = { url: buildUrl(putOptions.sightId) };
        return requestor.put(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), putOptions), callback);
    };
    const copySight = (postOptions, callback) => {
        const urlOptions = { url: buildUrl(postOptions.sightId) + '/copy' };
        return requestor.post(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), postOptions), callback);
    };
    const moveSight = (postOptions, callback) => {
        const urlOptions = { url: buildUrl(postOptions.sightId) + '/move' };
        return requestor.post(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), postOptions), callback);
    };
    const getSightPublishStatus = (getOptions, callback) => {
        const urlOptions = { url: buildUrl(getOptions.sightId) + '/publish' };
        return requestor.get(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), getOptions), callback);
    };
    const setSightPublishStatus = (putOptions, callback) => {
        const urlOptions = { url: buildUrl(putOptions.sightId) + '/publish' };
        return requestor.put(Object.assign(Object.assign(Object.assign({}, optionsToSend), urlOptions), putOptions), callback);
    };
    const buildUrl = (sightId) => {
        if (sightId !== undefined) {
            return options.apiUrls.sights + '/' + sightId;
        }
        return options.apiUrls.sights;
    };
    return Object.assign({ listSights,
        getSight,
        deleteSight,
        updateSight,
        copySight,
        moveSight,
        getSightPublishStatus,
        setSightPublishStatus }, shares.create(options));
}
// Export with legacy name for backward compatibility
exports.createSights = create;
